spring:
  application:
    name: Event-User-Service

  datasource:
    url: jdbc:postgresql://localhost:5432/EVENT_USER
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
